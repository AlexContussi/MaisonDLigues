{# empty Twig template #}
{% extends 'base.html.twig' %}
{% block body %}

<style>
.gridFormules{display:grid;
    grid-template-rows :repeat(3,1fr);
    grid-template-columns :repeat(2,1fr);
    margin: 3%;
}
.gridFormules div {

}
.noSelected{
    border: 1px solid gray;
    padding: 5%;
}


</style>
    
<div class="gridFormules">    
    {% for formule in formulesData %}
        <div class="noSelected">
            <div>{{formule.nom}}</div>
            <div>{{formule.adresse1}}</div>
            <div>{{formule.adresse2}}</div>
            <div>{{formule.cp}}</div>
            <div>{{formule.ville}}</div>
            <div>{{formule.tel}}</div>
            <div>{{formule.mail}}</div>
            {% for tarif in formule.tarifs %}
                <div>{{tarif.tarifNuite}} €/nuit</div>
            {% endfor %}
        </div>
    {% endfor %}
</div>

{% endblock %}
